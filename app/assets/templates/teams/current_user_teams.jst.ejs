<h1><%= current_user.escape("username")%>'s Teams</h1>
<% current_user.teams().each(function(team) { %>
  <div class="team-show-link" data-id="<%= team.id %>"><a href=""><%= team.escape("title") %><a></div><button class="team-leave-button" data-id="<%= team.id %>">Leave team</button><br>
<% }); %>